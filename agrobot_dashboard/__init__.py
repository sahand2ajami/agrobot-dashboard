"""Dual-robot teleoperation dashboard.

Package layout (dependency flow is strictly left-to-right):

    domain/    pure business logic — no I/O, no threads, no ROS
    (Phase 2 adds: config/, services/, adapters/, web/)
"""
